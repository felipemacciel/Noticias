export interface INoticiasProps {
  listID: string;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  context: any;
  titleUrl: string;
  titleSection: string;
  absoluteUrl: string;
}